# GitHub Search API 配置
# 复制此文件为 .env.local 并填入你的实际配置

# GitHub Personal Access Token (可选)
# 获取地址: https://github.com/settings/tokens
# 注意: 无需Token即可使用，但配置Token可提高API限制
VITE_GITHUB_TOKEN=your_github_token_here

# 可选配置
# 每页结果数量 (默认: 10)
VITE_RESULTS_PER_PAGE=10

# API 超时时间 (毫秒, 默认: 10000)
VITE_API_TIMEOUT=10000

# 搜索类型 (默认: repositories)
# 可选值: repositories, code, users
VITE_SEARCH_TYPE=repositories
